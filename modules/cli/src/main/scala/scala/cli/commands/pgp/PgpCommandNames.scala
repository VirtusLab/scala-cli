package scala.cli.commands.pgp

object PgpCommandNames {
  def pgpCreate = List(
    List("pgp", "create")
  )
  def pgpSign = List(
    List("pgp", "sign")
  )
  def pgpVerify = List(
    List("pgp", "verify")
  )
}
