name: scala-cli
version: "@LAUNCHER_VERSION@"
summary: Scala CLI combines all the features you need to learn and use Scala
description: |
  Scala CLI aims at making it easier to run, test, and package, Scala sources.
  It can:
    * compile Scala code
    * run it
    * package it as a JAR or in formats such as deb, rpm, MSI, …
    * fire up a REPL allowing you to manually run your code
    * compile and run tests suites
base: core18
grade: stable
confinement: classic

apps:
 scala-cli:
   command: scala-cli

parts:
  scala-cli:
    plugin: dump
    source-type: local
    source: .
